# Optional simulator runtime overrides for the AgentScience harness.
# Ensure the external Claude CLI is already installed and authenticated.

AGENT_MODEL=sonnet
AGENT_MAX_TURNS=12
AGENT_TIMEOUT_S=600
